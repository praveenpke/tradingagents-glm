# Secrets — NEVER commit
.env
.env.local
*.key

# Python virtual environment (machine-specific, large)
.venv/
venv/
env/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Runtime logs / scratch
run.log
*.log
dash_server.log

# Dashboard runtime state (local cache index)
dashboard_state.json

# Generated reports are local — keep the repo clean. A sample is force-added for the demo.
reports/

# OS
.DS_Store
Thumbs.db
